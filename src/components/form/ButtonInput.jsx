const ButtonInput = ({ buttonText }) => {
  return (
    <div className="contact__form--element">
        <button className="contact__btn">{buttonText}</button>
    </div>
  )
}

export default ButtonInput